import React from "react";
import ReactDom from "react-dom";
import "./AddProject.css";

function AddProject({ toggleModal }) {
  return (
    <div className="modalBackground">
      <div className="modelContainer">Modal</div>
      
    </div>
  );
}

export default AddProject;
